# Figma UI 개편 — API 호환성 정리

기준 Figma: `czUPZACUJUAo1IrNQj2x7x`, 프로토타입 섹션 `100:467`

## 조정하여 구현한 항목

### 1. 회원가입의 아이디 입력

- Figma 가입 흐름은 이름 → 이메일 → 비밀번호 → 입장 코드/조직 이름의 4단계입니다.
- 현재 `POST /auth/signup`은 별도의 `username`을 필수로 요구하고, `POST /auth/login`도 이메일이 아닌 `username`으로 로그인합니다.
- 따라서 4단계 구조는 유지하되 1단계에 **아이디** 입력을 함께 배치했습니다. 이를 제거하려면 API가 이메일 로그인을 지원하거나 서버가 username을 자동 생성해야 합니다.

### 2. ‘내 정보’ 탭

- Figma 하단 내비게이션에는 ‘내 정보’가 있지만 현재 API에는 프로필 수정/탈퇴 API가 없습니다.
- `GET /auth/me`로 이미 저장된 사용자·조직 정보를 보여주는 읽기 전용 화면과 로그아웃만 구현했습니다.
- 이름·이메일·비밀번호·조직 정보 편집은 대응 API가 추가되기 전까지 제공하지 않습니다.

### 3. 작업자 상태 지표

- Figma의 ‘작업 시간 / 몸 피로 / 상태 참고값 / 누적 피로’는 `GET /workers/status/today`의
  `total_work_seconds / latest_actual_borg_cr10 / state_borg_cr10 / daily_load`에 대응합니다.
- 실제 피로 응답이 없는 사용자는 `latest_actual_borg_cr10`이 `null`이므로 화면에는 `-`로 표시합니다.
- Figma의 양호·주의·위험 상태는 현재 클라이언트 기준값(상태 참고값 4 미만 / 4 이상 / 7 이상)으로 구분됩니다. 서버가 상태 등급을 직접 반환하지 않으므로 이 임계값은 프런트엔드 정책입니다.

### 4. 근무 인원 배지

- Figma 헤더의 ‘N명 근무’는 실시간 출근/근무 상태를 전제로 보이지만, 현재 API에는 근태 또는 현재 근무 여부 전용 필드가 없습니다.
- 기존 `getCurrentStaffStatus()`의 클라이언트 시간표 값을 유지했습니다. 여러 기기에서 동일한 실시간 값을 보장하려면 근무 상태 API가 필요합니다.

### 5. 경로 순서

- Figma는 건물 간 경로를 명시적인 순서로 보여줍니다.
- 현재 `GET /schedules/today` 응답에는 별도의 건물 순서 배열이 보장되지 않아, 기존처럼 일정 응답 순서와 `동선.건물명 → 건물명 → 설치장소 → 신청부서` 순의 대체값으로 구성합니다.
- 서버에서 슬롯별 `building_order` 같은 필드를 제공하면 화면과 경로 의미를 더 안정적으로 일치시킬 수 있습니다.

## API 변경 없이 유지한 동작

- 로그인, 회원가입, 비밀번호 재설정
- 신청서 목록·OCR 등록·수정
- 일정 최적화, 금일 일정 조회, 인원 추천·확정
- 출동 확정과 실내 네비게이션
- 작업 시간·피로 데이터 조회 및 학습 데이터 업로드

이번 개편은 위 API 함수의 요청 경로와 payload를 변경하지 않고 화면 구성과 내비게이션만 재구성했습니다.
